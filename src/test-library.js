'use strict';

define(['jquery', 'react', 'react-dom'], function($, React, ReactDOM) {
    return {
        version: "1.0.0",
        jquery: $,
        react: React,
        reactDOM: ReactDOM,
    }
})
